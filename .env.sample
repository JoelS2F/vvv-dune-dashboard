# Dune Analytics credentials for browser-based query deployment
# Copy to .env and fill in your values
DUNE_USERNAME=your-email@example.com
DUNE_PASSWORD=your-password

# Dune API key (for pipeline data export — free tier read access works)
DUNE_API_KEY=your-api-key-here
